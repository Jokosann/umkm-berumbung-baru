import { Outfit, DM_Sans } from 'next/font/google';

export const outfit = Outfit({ subsets: ['latin'], weight: ['200', '400', '500', '600', '800'] });
export const dmSans = DM_Sans({ subsets: ['latin'] });
